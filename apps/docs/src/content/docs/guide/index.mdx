---
title: 指南
description: Kiln 的数据流、凭据模型与引擎变体总览，以及本区各专题页面的入口。
sidebar:
  order: 1
---

本区讲清楚 Kiln 运行时到底做了什么：上游从哪里进来，媒体在哪一步被打包，播放地址以什么形态发出去，以及每一段路由分别认哪种凭据。想先把服务跑起来，请从 [快速开始](/start/) 开始。

## 数据流

一条频道从上游到播放器要经过四步，每一步都可以单独配置：

```text
上游（HLS / DASH）
      │
      ▼
  拉流  pull       按需启动，无人观看时回收上游连接
      │
      ▼
  打包  packager   HLS 同源分片代理；DASH 本地解密后重封装为 HLS
      │
      ▼
  分发             播放地址、M3U 播放列表、EPG、管理控制台
```

拉流与打包的生命周期由会话管理器统一持有：同一个频道无论有多少观众，只维持一份上游连接和一份打包产出。

## 四种凭据

每种凭据只覆盖自己的那一段，互相之间不能提权。

- **公开端点**：不需要凭据，覆盖 `/healthz`、`/readyz`、`/metrics`、`/v1/epg.xml`、`/v1/logo/{id}` 以及做了限流的 `POST /v1/auth/login`。
- **登录会话 JWT**：口令登录换取的 Ed25519 签名令牌，服务管理控制台以及只认会话的 `/v1/playlist.m3u`。
- **管理员 API Token**：给脚本和命令行用的长期凭据，明文只展示一次，按 `read`、`write`、`delete`、`refresh` 分权，且只能访问已登记的管理路由。
- **路径式播放密钥**：写在 URL 路径里的 `/p/{token}/...`，可限定频道范围、随时撤销，专门用来把播放列表交给播放器。

完整的路由与权限矩阵见 [接口参考](/reference/api/)，凭据的签发与轮换见 [鉴权与凭据](/guide/auth/)。

## 引擎与变体

打包引擎有 `native`、`ffmpeg` 与 `auto` 三种：`native` 全程由 Go 原生实现，不需要外部依赖；`ffmpeg` 调用外部二进制；`auto` 优先原生并在源不受支持时回退。频道级的 `packager` 字段可以单独覆盖全局取值。

运行时另有 `lite`、`core` 与 `full` 三个发行变体，能力边界不同但共用同一套配置模型：显式写在配置里的引擎取值始终优先，同一份配置不会因为换了变体而改变行为。

## 继续阅读

<CardGrid>
  <Card title="频道与上游">
    频道模型逐字段说明、上游定义、启停与批量导入导出。[打开](/guide/channels/)
  </Card>
  <Card title="播放与分发">
    播放地址形态、鉴权方式、播放密钥与按需频道的冷启动行为。[打开](/guide/playback/)
  </Card>
  <Card title="鉴权与凭据">
    口令、会话 JWT、管理员 API Token 与播放密钥的签发和轮换。[打开](/guide/auth/)
  </Card>
  <Card title="媒体引擎">
    原生管线与兼容回退的取舍，以及打包相关的全部预算参数。[打开](/guide/media-engine/)
  </Card>
  <Card title="变体与轨道">
    发行变体的能力边界，以及分辨率、音轨与字幕的精确选择。[打开](/guide/variants/)
  </Card>
  <Card title="节目单 EPG">
    XMLTV 源、刷新策略、频道匹配与台标候选源。[打开](/guide/epg/)
  </Card>
  <Card title="出站代理">
    按域名或频道路由 HTTP 与 SOCKS，以及播放列表改写策略。[打开](/guide/proxy/)
  </Card>
  <Card title="管理控制台">
    Web UI 的日常操作、搜索与批量维护。[打开](/guide/admin/)
  </Card>
  <Card title="运维">
    健康检查、指标、日志与资源自适应。[打开](/guide/operations/)
  </Card>
  <Card title="故障排查">
    常见症状与定位顺序。[打开](/guide/troubleshooting/)
  </Card>
</CardGrid>
